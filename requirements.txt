# ─── JARVIS Requirements ───────────────────────────────────────────────────────
# REQUIRED (core functionality)
openai>=1.30.0          # GPT AI brain
psutil>=5.9.0           # System monitoring (CPU/RAM/disk)
pyautogui>=0.9.54       # Screenshot capture

# OPTIONAL (voice features - install if you want voice I/O)
# SpeechRecognition>=3.10.0
# pyttsx3>=2.90
# pyaudio>=0.2.14       # Required by SpeechRecognition for microphone

# OPTIONAL (clipboard monitoring)
# pyperclip>=1.8.2

# OPTIONAL (enhanced screenshot if pyautogui fails)
# Pillow>=10.0.0

# ─── Install all required ──────────────────────────────────────────────────────
# pip install openai psutil pyautogui

# ─── Install with voice support ───────────────────────────────────────────────
# pip install openai psutil pyautogui SpeechRecognition pyttsx3 pyaudio

# ─── Windows users: if pyaudio install fails ──────────────────────────────────
# pip install pipwin
# pipwin install pyaudio
